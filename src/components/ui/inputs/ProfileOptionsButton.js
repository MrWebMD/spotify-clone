import Button from "./Button";
import classes from './ProfileOptionsButton.module.css';

const ProfileOptionsButton = (props) => {
  return <Button>Profile</Button>
}

export default ProfileOptionsButton;